@namespace BlazorBeerCss
@using Extensions;
@using Helpers;

<div class="page @(Active ? "active" : "")">
    @ChildContent
</div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    [Parameter]
    public bool Active { get; set; } = true;
}
